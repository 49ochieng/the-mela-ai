# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
.eggs/
*.egg
.mypy_cache/
.pytest_cache/
htmlcov/
.coverage

# Virtual environments
.venv/
venv/

# Node
node_modules/
.next/
out/
.turbo/
coverage/

# Environment variables
.env
.env.local
.env.production
.env.staging

# IDE
.vscode/settings.json
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
ehthumbs.db

# Azure
.azure/
*.publish.xml

# Logs
*.log
logs/

# Build artifacts
*.zip
*.tar.gz
*.tsbuildinfo

# Databases
*.db
*.sqlite
*.sqlite3

# Temp / scratch files (Claude Code working files)
tmpclaude-*
nul
null
# (junk artifact dirs removed — patterns were invalid globs)

# Docker
.docker/

# Secrets (never commit)
*.pem
*.pfx
*.key
*.cert
credentials.json
**/secrets/
azure-credentials.json
service-principal.json

# env/ secrets folder — ignore all .env.* files; only *.sample templates are committed
env/.env.*
!env/*.sample

# Environment files with secrets (all directories)
backend/.env
frontend/.env.local
frontend/.env.production.local
**/*.env
**/.env.*
!**/.env.example
!**/.env.sample
!**/.env.*.sample

#claude code
.claude/

# Azure Developer CLI — environment state (contains subscription IDs, preflight results)
.azure/

# Bicep compiled ARM templates
*.json.compiled

# scripts output (auto-generated by scan-resources)
scripts/resource-inventory.json

# Deployment artifacts
artifacts/
*.zip

# Local deploy/diagnostic artifacts (never commit — large logs & generated output)
backend-deploy-staging/
backend_logs_all_scan_extract/
backend_logs_now/
startup_diag_report.txt
startup_diag_report_localsnapshot.txt
infra/main.json

# Agent runtime data (user uploads / persisted memory — not source)
backend/data/agent_memory/
