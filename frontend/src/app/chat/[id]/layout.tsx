/**
 * Mela AI - Chat [id] Layout
 *
 * Inherits auth guard from parent /chat layout.
 */

'use client';

export default function ChatIdLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
